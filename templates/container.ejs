import React, { Component } from 'react';
import { ScrollView, Text } from 'react-native';
import { connect } from 'react-redux';
// Add Actions - replace 'Your' with whatever your reducer is called :)
// import YourActions from 'rdx/your-redux';

// Styles
import styles from './<%= props.fileName %>-styles';

class <%= props.name %> extends Component {
  // constructor (props) {
  //   super(props)
  //   this.state = {}
  // }

  render() {
    return (
      <ScrollView style={styles.container}>
        <Text><%= props.name %> Container</Text>
      </ScrollView>
    );
  }
}

const mapStateToProps = () => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

const Connected<%= props.name %> = connect(mapStateToProps, mapDispatchToProps)(<%= props.name %>);

export { Connected<%= props.name %> as <%= props.name %> };
